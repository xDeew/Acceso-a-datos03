Problemas al crear un JAR funcional con Maven.

Al intentar ejecutar el JAR desde Maven da errores; por ejemplo que el panel no se inicializó (error desde consola) y cada vez que se solventa uno sale otro. Es por ello, que el JAR no es funcional, pero debido a la complejidad actual que tengo para generar el JAR desde Maven.